## Summary
<!-- 1–3 bullets -->

## Checklist
- [ ] `python3 scripts/update_code_snapshot.py verify` (skip only if intentional re-pin — document in PR)
- [ ] `python3 -m pytest -q` if contract/tests touched
- [ ] `cd web && npm run build` if site touched
- [ ] CHANGELOG / README updated if user-facing
- [ ] No secrets committed

## Test plan
- [ ]
